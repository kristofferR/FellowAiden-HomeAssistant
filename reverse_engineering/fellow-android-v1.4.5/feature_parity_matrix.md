# Feature Parity Matrix

| Mobile Capability | HA Relevance | Current HA State | Mobile Evidence Delta | Next Step | Blocking Dependency |
| --- | --- | --- | --- | --- | --- |
| Cloud auth/session bootstrap | High | Config flow uses `/v1/auth/login`; refresh uses `/v1/auth/refresh`. | Runtime confirms `/v2`, login `timezone`, success `201`, and `/auth/refresh-token` returning `201`. | Migrate the client with explicit v1 compatibility only where required. | Runtime contract captured; implementation and compatibility tests remain. |
| Device inventory and telemetry | High | Implemented via coordinator polling and sensors/binary sensors/selects. | Live v2 inventory/detail exposed 57/64 fields. Brew phases, pump, shower head, connectivity, cleaning/rinsing, firmware state, capability flags, and sync state are not fully used. | Fix diagnostics redaction, then add the highest-value disabled-by-default entities from `runtime_telemetry_delta.md`. | Non-null temperature and positive update/maintenance cases remain unavailable. |
| Profiles list/create/delete/detail | High | List/create/update/delete/share are present in the client and services. | Runtime confirms v2 list/detail/create/update/delete. Captured mutation bodies did not include `settingsVersion`; public fetch remains static-only. | Migrate CRUD to v2 using the captured schemas and test fractional/cold-profile models. | Sharing and conflict behavior remain runtime-unverified. |
| Schedules list/create/delete/toggle | High | Implemented via services and vendored schedule models. | Runtime confirms create/full update/enabled-only update/delete and the five request fields. | Migrate schedule CRUD to v2 and align model fields/status handling. | No blocker for captured CRUD semantics. |
| Direct brew control and cleaning flows | High | Not implemented. | Instant `PATCH /start?confirm=true` succeeded without a body and produced live phase transitions. `/stop` returned 400 throughout active telemetry. Clean/rinse remain static-only. | Do not expose remote start until stop/cancel behavior is reliable; investigate the server/device inconsistency first. | Reliable cancellation and maintenance traces are missing. |
| Device patch/settings customization | Medium-High | Generic device patch exists in the vendored client but is not broadly exposed as HA entities. | Live detail fields and capability flags are captured. A direct `doCancel` patch was rejected as an unknown property. | Limit patch models to app-confirmed writable settings; do not treat telemetry/command flags as generic writable properties. | Writable field allowlist is incomplete. |
| BLE claim and association | Medium | Not implemented and currently outside the cloud-only integration model. | Static evidence confirms RN BLE stack and companion scan support. | Document only until a hardware-backed provisioning effort is needed. | Blocked by missing hardware and runtime target. |
| Wi-Fi onboarding / local provisioning | Medium | Not implemented and currently outside the cloud-only integration model. | Static evidence suggests local Wi-Fi/AP negotiation via RNWifi plus provisioning posts. | Treat as separate local-transport project after hardware is available. | Blocked by missing hardware and runtime target. |
| Firmware/update status and flows | Medium | Not implemented. | Runtime confirms `GET /devices/{id}/updates` plus `firmwareUpgradeRequired`; only a no-update case was available. | Add read-only update state after a positive case is captured; keep install static-only. | Positive update and installation traces are missing. |
| Mobile push notifications | Low-Medium | Out of scope for HA entity parity. | Runtime shows FCM registration and repeated receiver deliveries around brew changes, without a visible foreground notification. | Use coordinator polling for HA; decode FCM only if event latency becomes a separate project. | Not required for current HA scope. |
| Share links / drops / public content | Low | Out of scope. | Deep links and share-style profile routes are present in the mobile app. | Ignore unless profile sharing needs to be mirrored in HA services. | No blocker. |
| Espresso Series 1 product flows | Low | Out of scope for this Aiden-focused integration. | The mobile artifact clearly contains Series 1 content and troubleshooting copy. | Keep separated from Aiden parity work. | No blocker; excluded by scope. |

Reference: `evidence/current_integration_state.md` plus the generated cloud/local catalogs.
